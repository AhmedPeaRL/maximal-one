from __future__ import annotations
from scripts.prepare_canonical_inputs import main

if __name__ == "__main__":
    print(
        "ℹ️ expand_dataset.py is a compatibility wrapper."
    )

    print(
        "ℹ️ Canonical derived inputs are generated only by:"
    )

    print(
        "   scripts/prepare_canonical_inputs.py"
    )

    main()
