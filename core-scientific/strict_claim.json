{
  "title": "Constrained Spectral Persistence Hypothesis",

  "statement": "Deterministic spectral persistence signatures emerge reproducibly across selected heterogeneous temporal datasets and remain distinguishable from trivial stochastic null models under constrained validation.",

  "dataset": {
    "primary": "sunspots_global",
    "secondary_required": true,
    "adversarial_rejection_required": true,
    "cross_domain_required": true
  },

  "method": {
    "pipeline": [
      "dataset_preprocessor",
      "windowed_spectral_analysis",
      "bootstrap_estimation",
      "cross_domain_validation",
      "adversarial_filtering",
      "external_reproduction"
    ],
    "seed": 42,
    "deterministic": true
  },

  "expected_result": {
    "alpha_range": [0.05, 3.0],
    "preferred_band": [0.6, 2.2],
    "max_cross_domain_std": 1.2,
    "max_sigma": 0.85,
    "max_method_delta": 0.5,
    "min_separation_z": 2.5
  },

  "falsification": {
    "condition": "If alpha exits [0.05,3.0] OR sigma > 0.85 OR cross-domain std > 1.2 OR FFT/Welch disagreement > 0.5",
    "action": "Mark system as falsified"
  },

  "externality": {
    "independent_replay_required": true,
    "fingerprint_match_required": true
  }
}
