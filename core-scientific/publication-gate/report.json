const fs = require('fs');
const { canonicalStringify } = require('../utils/canonical-json');

function writeReport(data) {
  const output = canonicalStringify(data);
  fs.writeFileSync(
    './core-scientific/publication-gate/report.json',
    output
  );
}

module.exports = { writeReport };
