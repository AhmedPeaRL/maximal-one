# Failure Condition (Closed Definition)

The attractor claim is considered invalid if:

|z| ≥ 1

where:

z = (α - 0.5) / σ

This defines statistical deviation beyond 1 standard deviation.

CI automatically fails if condition violated.

No manual override allowed.
