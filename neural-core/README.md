NEURAL CORE

This directory defines the existence of a core,
not its activation.

No parsing.
No inference.
No response.

The repositories are bound conceptually,
not computationally.

Any attempt to execute this core
without explicit authorization
violates system sovereignty.
